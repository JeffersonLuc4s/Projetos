version: "3.9"

services:
  bot:
    build: .
    container_name: makeup-deals-bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./data:/app/data  # SQLite database persiste aqui
